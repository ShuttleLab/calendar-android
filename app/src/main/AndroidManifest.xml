<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 唯一需要的权限:取一次公开的节假日安排(见 data/HolidayRepo.kt)。
         农历、节气、节日全部在本机计算,不需要任何权限;没有通知、没有闹钟、没有后台、
         不读日历、不读存储 —— 这一行就是全部。
         EN: the only permission needed: fetching the public holiday schedule once
         (data/HolidayRepo.kt). The lunar calendar, solar terms and festivals are computed
         locally and need nothing; there are no notifications, alarms, background work,
         calendar reads or storage reads. This line is the whole list. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalendarShuttle">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|locale|layoutDirection|fontScale|density|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
